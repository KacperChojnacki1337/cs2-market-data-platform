version: 2

sources:
  - name: steam_raw
    project: steam-tracker-portfolio
    dataset: "{{ 'steam_raw_dev' if target.name == 'dev' else 'steam_raw' }}"
    tables:
      - name: assets_history
      - name: prices_history
      - name: skinport_prices_history
      - name: exchange_rates
      - name: sales_history
